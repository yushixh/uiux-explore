async function HE(e){let{BLOCKS:t}=await BE(async()=>{let{BLOCKS:e}=await import(`./blocks-CntrDPKE.js`);return{BLOCKS:e}},[]),n=t[e.id];if(!n)return null;let r=VE(e),i=[];if(i.push(`Add the "${e.name}" component from Bencho to my project.`),i.push(``),i.push(`It is MIT licensed — bencho.dev/licence. Please:`),i.push(``),i.push(`1. Create the component at a sensible path for this`),i.push(`   project, named ${r}, from the source below.`),n.deps.length?i.push(`2. Install what it needs: npm i ${n.deps.join(` `)}`):i.push(`2. It needs nothing beyond React.`),i.push(`3. Add the CSS to the project's stylesheet.`),n.tokens.length){i.push(``),i.push(`4. THE PART THAT NEEDS YOUR JUDGEMENT. The CSS reads`),i.push(`   these custom properties and does not define them:`),i.push(``);for(let e of n.tokens)i.push(`     ${e}`);i.push(``),i.push(`   They are Bencho's design tokens. Map each one to`),i.push(`   whatever this project already uses for the same`),i.push(`   job — its own ink, ground, surface and UI font —`),i.push(`   rather than defining them as new globals. If the`),i.push(`   project has no equivalent, define it locally on`),i.push(`   the component's own root so nothing leaks out.`),i.push(`   Anything ending -rgb wants three bare numbers,`),i.push(`   because the CSS builds rgba() from it.`)}return n.stubs.length&&(i.push(``),i.push(`5. ${n.stubs.join(` and `)} ${n.stubs.length>1?`are stubs`:`is a stub`}`),i.push(`   — Bencho's own pictures are not licensed to`),i.push(`   travel. Point it at this project's images, or`),i.push(`   leave the placeholder and tell me.`)),i.push(``),i.push(`Keep the comments. They say why the numbers are what`),i.push(`they are, and they are most of what makes this worth`),i.push(`copying rather than rewriting.`),i.push(``),i.push(`--- ${r}.tsx ---`),i.push(``),i.push(n.tsx.trim()),i.push(``),i.push(`--- css ---`),i.push(``),i.push(n.css.trim()),i.push(``),i.join(`
`)}