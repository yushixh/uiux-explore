# 卡片资源库验收（当前版本）

- 左侧为组件 98、设计参考 1,692、源码与资料 186。旧 AccentContext 链接进入资料区；静态 Text / Context 不出现在组件区。
- 组件改为卡片画廊，原始版和本项目版使用独立本地 URL。每页 12 张、按可见位置加载 iframe；参考区无组件 iframe。
- 原始版使用本地源码、样式、字体和素材。Bencho 原始版完整保留原 bundle 依赖，替换列表为空；本项目版的依赖适配、占位配图及键盘增强独立保留。
- 浏览器检查页 `/tests/archive-previews.html` 可检查双版本渲染、图片、横向溢出和远程请求。下方保留此前版本的历史检查记录，旧布局和 110 预览数量不代表当前画廊。

## 当前版本实测结果

- **196 / 196 双版本预览通过**：98 张卡片 × 原始版/本项目版；在 390 × 350 预览框检查运行 ready、渲染节点、图片完整、无页面横向溢出、无远程资源请求。测试页仍可重新运行。
- Bencho Carousel 原始画布最初超出预览边界；按原站舞台的 overflow 规则约束外框后，包含该项的全量复验通过。
- 桌面 1280 像素下为卡片网格，无列表/详情双列。设计参考区有 12 张当页卡片、0 个交互 iframe。
- 390 像素响应式页：内容宽 375 / 375，卡片单列 351 像素。360 像素响应式页：内容宽 345 / 345，单列 321 像素；差值来自系统滚动条，无页面横向溢出。
- 卡片中的 Create menu 本项目版通过 ArrowDown 打开；切原始版再返回，本项目版保持展开；Escape 收起。源码弹窗可通过按钮/Enter 打开、Escape 关闭，焦点返回卡片源码按钮，360 像素下可正常查看和滚动代码。
- Halaska 原始 Slider 键盘调整 35 → 36 → 35；Torph 原始 Spring 点击 bouncy 显示 stiffness 260 / damping 12，再选 snappy 恢复 200 / 20，选择反馈与动画变化一致。
- AccentContext 的旧深链接自动归入源码与资料；分区筛选和卡片链接保留稳定 ID，未丢弃归档条目。
- 完整性测试通过：1,976 条目、4,167 文件哈希、1,859 引用文件、85 份源码语法检查；另外断言双版本入口、分区分类、Bencho 29 项原始依赖无替换。
- `npm run archive:previews` 可完整重建；TypeScript 与生产构建通过。仅保留原有 Image 动效包大小警告。
- 验证范围为加载、代表性交互与布局；没有声称覆盖所有第三方业务分支或所有状态的逐像素对比。

## 历史记录：归档与初版预览

# 资源库验收 · 2026-09-15

## 覆盖范围

- 七来源目录、公开源码/Prompt 本地保存、明确的缺口状态。
- 来源、层级、内容类型、用途分类与多词搜索；空结果恢复。
- 单资源预览/源码/Prompt、文件切换、复制、下载链接、切换后保留文件选择。
- Kage 整页/组件父子关系、同类实现、Torph 多框架入口。
- 目录分页、深链接、浏览器历史、工作台/资源库切换、范围说明对话框。
- 桌面和 390 像素窄屏下的布局、点击、键盘焦点和 Escape。
- 非理想路径：无搜索结果、源码未公开、需要 Plus、没有生成 Prompt 的工具库。

## 数据和构建

`npm run test:archive` 验证 1,976 个唯一条目、来源与分类、父子引用、顶层整页、所有引用文件存在、全量文件 SHA-256、计数以及 72 份提取源码的语法。

- Kage：1,358 个组件参考 + 257 个整页 Prompt。
- Bencho：29 项目录，12 套完整源码和 17 项片段；另为 17 项接入公开网页运行实现，全部 29 项可交互。
- VantaUI：87 项，12 个免费组件的公开源码，75 项 Plus 内容未获取。
- Prompt：1,639 份；其中 VantaUI 的 12 份是原站询问页面内容的简短模板。
- 无完全相同的源码条目或 Prompt 条目；共享 Kit 以导出名区分。

`npm run build` 包含 TypeScript 严格检查和生产构建。归档目录从 Tailwind 扫描中排除；上游代码的 class 名称不会增加主站 CSS。原有 Image 动效包大于 500 KB 的警告仍保留。

## 浏览器实测

Codex 浏览器，桌面约 1280 × 720；窄屏 390 × 844。窄屏文档宽 375（另有 15 像素系统滚动条），scrollWidth 同为 375，无页面横向溢出。额外保留 `/tests/archive-responsive.html` 作为 390/360/1280 宽度的人工复验入口。

- 原有 93 个和新增 17 个本地预览逐项加载，均产生真实渲染节点并进入 ready 状态。Bencho 的图像空桩通过预览适配器填入自制占位图，原始源码不改写。
- 修正了初始预览中 Magnetic Select 图片空数组引起的错误，以及 Progress 容器宽度为零的问题；修正后复验通过。
- Bencho Magnetic Select 选择第二项再返回第一项，`aria-checked` 与视觉选中一致。自动化在窄屏嵌套 iframe 点击上遇到小数坐标限制，使用同一个预览的独立页面完成点击复验。
- Halaska 预览适配器为受控输入提供状态回调；Slider 键盘调整 55 → 56 → 55，SwitchToggle 关闭 → 开启 → 关闭，均验证通过。
- Typehug 修改输入后实时显示 `20⍽USD`；Torph 切换文字再返回初始文字，输出与按钮状态一致。
- 源码从 TSX 切到 CSS，复制按钮显示“已复制”，下载链接指向当前本地文件；预览/源码来回切换保留 CSS 文件选择。
- Bencho Prompt 为实际原站模板；Kage Linear Hero 原文 Prompt 长度 5,848 字符，内容已读取。
- 搜索无匹配结果显示 0 项；Escape 清空恢复；VantaUI + 完整源码筛选显示 12 项；Kage + 整页显示 257 项。
- Linear Hero 跳转到所属 Linear 整页，整页无父级，列出 12 个内部组件。
- 范围说明打开、Escape 关闭，窄屏亦可打开；工作台/资源库切换正常，原七组件入口可用。
- 桌面和手机分别查看了预览与源码状态。列表在手机上横向滚动，代码在自身容器内滚动；未发现主内容横向溢出或按钮被遮挡。
- 原有 `/tests/behavior.html` 的 **25 项集成检查全部通过**，包括配色、业务状态、低动态、Gooey、Orb 和销毁清理。

## Bencho 交互预览补齐

- 原先 17 项只显示设计片段提示；现从已固定哈希的公开网页 bundle 静态提取组件和依赖声明，编译成独立预览。全部 29 个 Bencho 条目均可交互，总预览数为 110。
- 新增 17 项分别在 1280 和 390 像素视口渲染检查，全部 ready、无页面横向溢出、无控制台错误。
- Create menu：鼠标打开、选择 Document 收起、点击外部收起；方向键打开并移动到 Spreadsheet，Escape 收起后焦点返回 Create；折叠菜单不进入键盘焦点顺序。
- Reorder list：将 Mara Quinn 从第一行拖到第四行，实际视觉顺序更新。Drag stepper：24 → 25 → 24。Inline confirm：Delete file → Keep / Delete → Keep 返回初始状态。
- 资源库内嵌菜单在桌面和窄屏可通过键盘展开；350 像素高的窄屏 iframe 无多余垂直滚动。浏览器自动化对小数坐标 iframe 点击有限制，鼠标行为在同一个预览的独立页面验证。
- Bencho + 可交互预览显示全部 29 项；源码页能读到原作者署名、运行入口和构建产物说明，再切回预览可正常使用。
- 完整性检查覆盖 4,006 个文件哈希、1,844 个引用文件、72 份源码语法检查，并要求全部 Bencho 条目有真实预览文件。生产构建通过。

## 明确边界

- 不宣称 1,976 项都是可直接运行的组件；条目包含整页设计、参考产品、片段与未获取的付费内容。
- 110 个预览做了渲染检查；并未对每个第三方组件的全部业务分支做完整验收。
- 远程参考图需要网络，加载失败有回退说明；本地代码和 Prompt 不依赖这些图片。
- 未进行真实手机、Safari 或 Firefox 专项验证。
